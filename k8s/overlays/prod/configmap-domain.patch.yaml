# Strategic-merge patch — overrides only the domain-dependent keys from
# k8s/base/configmap.yaml's placeholder values. Edit every "app.yourdomain.com"
# below to your real domain before deploying.
apiVersion: v1
kind: ConfigMap
metadata:
  name: mindforge-config
  namespace: mindforge
data:
  DOMAIN: "app.yourdomain.com"
  FRONTEND_URL: "https://app.yourdomain.com"
  BACKEND_URL: "http://backend:8080"
  NEXT_PUBLIC_API_URL: "https://app.yourdomain.com"
  NEXT_PUBLIC_APP_URL: "https://app.yourdomain.com"
  NEXT_PUBLIC_MEDIA_URL: "https://app.yourdomain.com"
  NEXT_PUBLIC_LAB_PROXY_URL: "wss://app.yourdomain.com/lab-ws"
  MINIO_PUBLIC_ENDPOINT: "app.yourdomain.com"
