# Ponytail Audit

Whole-repo over-engineering scan (not a correctness/security/performance review).
Generated by `/ponytail-audit` on 2026-08-01, ranked biggest cut first.

- **delete** `seed_hiring_assessment.py` — never invoked by CI/Makefile/docs, speculative demo script. `scripts/seed_hiring_assessment.py:1-395`
- **delete** `ctxClaims` helper — `httputil.Claims(w, r)` already wraps `auth.GetClaims` with the unauthorized response; this duplicate is reimplemented across 26 handler files. `internal/assessment/handler.go:1034` (+25 more)
- **delete** `writeDomainError` — collapse to one error-type switch/registry instead of a copy in each handler package. `internal/assessment/handler.go:1127`, `internal/calendar/handler.go:415` (+14 more)
- **shrink** `lab-node-web`/`lab-python-web` `entrypoint.sh` + `app-runner.sh` — byte-identical in both images; extract to a shared base image or parent Dockerfile. `lab-images/lab-{node-web,python-web}/{entrypoint,app-runner}.sh`
- **delete** `start-dev.ps1` — documented in README but never called; `make dev-up` + manual frontend start already covers it. `scripts/start-dev.ps1:1-81`
- **delete** `writeError` — reuse `writeDomainError`. `internal/experience/handler.go:27`, `internal/feedback/handler.go:38` (+4 more)
- **stdlib** `isUniqueViolation` — `authz/admin_repo.go:96` does a fragile string match while `gitlab/repo_team.go:169` and `profile/repo.go:191` correctly use `errors.As`/`pgconn.PgError`; consolidate to one shared helper.
- **delete** `CoursePurchaser` interface — single implementation (`mentoring.Service.StartCheckout`); inline if it has one caller. `internal/courses/handler.go:68`
- **shrink** `NewExecutor` factory — inline the `unavailableExecutor{}` check at the call site. `internal/assessment/executor.go:59`
- **stdlib** repeated encode-error-logging in `WriteJSON`/`WriteError`/`WriteFieldErrors` — one shared wrapper. `internal/httputil/response.go:10,19,29`
- **delete** `EMPTY_SCENE` — unused exported constant. `frontend/lib/design.ts:40`

Frontend contexts, hooks, and single-export utility files (`FeatureFlagProvider`,
`CurrencyProvider`, `TerminologyProvider`, `LabProvisioningProvider`,
`progress.ts`, `permissions.ts`, `classify.ts`, `file-icon.ts`, etc.) were
checked and judged appropriately scoped — not flagged.

---

**net: ~-700 to -900 lines, -2 files (`seed_hiring_assessment.py`, `start-dev.ps1`) minimum, plus consolidation of 26+16+6 duplicated handler-error call sites.**
