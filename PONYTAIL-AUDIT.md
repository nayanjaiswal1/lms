# Ponytail Audit

Whole-repo over-engineering scan (not a correctness/security/performance review).
Generated by `/ponytail-audit` on 2026-08-01, ranked biggest cut first.
Fixed items removed 2026-08-02 (see `PONYTAIL-DEBT.md` for unrelated deferrals).

- **shrink** `lab-node-web`/`lab-python-web` `entrypoint.sh` + `app-runner.sh` — byte-identical in both images; extract to a shared base image or parent Dockerfile. `lab-images/lab-{node-web,python-web}/{entrypoint,app-runner}.sh`. Deferred: no committed build script/CI shows how these images are actually invoked (`docker build <dir>` context assumed but unverified), so changing the build context risks breaking an out-of-repo pipeline. Confirm the real build command before restructuring.

Not flagged after investigation (kept as-is):
- `CoursePurchaser` interface (`internal/courses/handler.go:68`) — looked like a single-impl interface to inline, but it exists specifically to break an import cycle (`mentoring` imports `courses`, so `courses` can't import `mentoring`'s concrete type back). The interface is the fix, not the bloat.
- `NewExecutor` factory (`internal/assessment/executor.go:59`) — called from 3 separate places (`internal/api/router.go`, `internal/assessment/routes.go`, `internal/interviewprep/routes.go`); inlining would triple the Piston/Judge0 config-branching logic it exists to share.

Frontend contexts, hooks, and single-export utility files (`FeatureFlagProvider`,
`CurrencyProvider`, `TerminologyProvider`, `LabProvisioningProvider`,
`progress.ts`, `permissions.ts`, `classify.ts`, `file-icon.ts`, etc.) were
checked and judged appropriately scoped — not flagged.
