# Ponytail Debt Ledger

Every `ponytail:` shortcut comment in the repo, collected so a deferral can't
quietly become permanent. Generated by `/ponytail-debt` on 2026-08-01.

Format: `file:line, what was simplified. ceiling: the limit named. upgrade: the trigger to revisit.`
Rows with no named upgrade condition are tagged **no-trigger** — these are the ones that silently rot.

## Backend

- `backend/cmd/labproxy/preview.go:27`, one shared port cookie for preview assets. ceiling: only one port's absolute-path assets resolve at a time. upgrade: subdomain-per-port routing.
- `backend/cmd/labproxy/preview.go:39`, all-digits top-level path treated as a port. ceiling: single-port app with numeric path segment misread as a port. upgrade: subdomain-per-port routing.
- `backend/internal/assessment/repo_offline_tests.go:19`, no test-template table for offline test scores. ceiling: can't reuse a test name/rubric across batches. upgrade: add a template table if teachers need cross-batch reuse.
- `backend/internal/calendar/recurrence.go:98`, fixed 2000-step scan cap instead of closed-form window jump. ceiling: ~5.5 years of daily occurrences. upgrade: raise `maxOccurrenceScan` if a real series exceeds that span.
- `backend/internal/courses/repo.go:1395`, hard 100-row cap on pending proposals, no pagination. ceiling: no offset/limit params. upgrade: add pagination if a course exceeds 100 pending proposals.
- `backend/internal/gitlab/client.go:179`, GitLab Premium/Ultimate collapsed to one "premium" tier. ceiling: can't distinguish without an admin license read. upgrade: add admin `GET /license` call when a real Ultimate-gated feature needs it.
- `backend/internal/gitlab/handler_connection.go:31`, no full SSRF denylist on admin-supplied GitLab base URL. ceiling: only rejects malformed input. upgrade: add denylist when this app takes unauthenticated/student-facing URL intake.
- `backend/internal/gitlab/originality.go:146`, 512KB per-file originality-check cap. ceiling: larger files skipped. upgrade: raise if a legitimate large source file gets skipped.
- `backend/internal/gitlab/originality.go:151`, 300-file-per-project originality cap. ceiling: O(files_a × files_b) comparison cost. upgrade: raise, or switch to MinHash bucketing, if a real repo exceeds the count.
- `backend/internal/labs/service_ports.go:34`, port scan reports ports only, no process names. ceiling: no /proc fd inode walk. upgrade: add when a user actually asks for it.
- `backend/internal/labs/service_ports.go:65`, no server-side rate limit on port listing. ceiling: 5s client poll + exec cost is the only throttle. upgrade: add a Redis token bucket if abuse shows up.
- `backend/internal/mentoring/service_purchase.go:88`, coupon global-cap check is a soft (non-locking) read. ceiling: concurrent buyers can slip past the global cap (per-user cap stays atomic). upgrade: `FOR UPDATE` lock on the coupon row if an exact cap is ever required.
- `backend/internal/mcpconnect/tools.go:1151`, `qualityFromBool` collapses SRS review to pass/fail. ceiling: no partial-credit quality levels. upgrade: caller needing finer-grained recall should call `POST /api/srs/review` directly with a 0–3 quality.

### No-trigger (backend)

- `backend/internal/courses/service_test.go:5` — no DB test infra for domain packages; only pure-Go branches get covered. No condition named for when to build real DB test infra.
- `backend/internal/mcpconnect/action_log_test.go:8` — same DB-test-infra gap, no revisit condition.
- `backend/internal/roadmap/service_test.go:8` — same gap, no revisit condition.
- `backend/internal/whatnow/service_test.go:5` — same gap, no revisit condition.
- `backend/internal/mcpconnect/tools.go:830` — MCP `create_interview_prep_plan` has no Revert (would let daily-cap bypass via create+revert); stated as permanent, no future condition.
- `backend/internal/mcpconnect/tools.go:882` — no Revert once AI grading has run; permanent, no condition.
- `backend/internal/mcpconnect/tools.go:925` — no Revert once code has executed against test cases; permanent, no condition.
- `backend/internal/mcpconnect/tools.go:983` — no Revert for system-design attempts (repo has no delete method); permanent, no condition.
- `backend/internal/mcpconnect/tools.go:1097` — no Revert once AI feedback is generated/billed; permanent, no condition.
- `backend/internal/mcpconnect/tools.go:1143` — no Revert once AI reply is generated; permanent, no condition.

## Frontend

- `frontend/app/(app)/cohort-groups/create-cohort-group-form.tsx:43`, parent-group picker excludes only the group itself, not descendants. ceiling: picking a descendant fails server-side with a less-friendly cycle error. upgrade: filter the whole subtree client-side if that surfaces as a real issue.
- `frontend/components/projects/my-checkpoint-list.tsx:16`, approvals shown as a plain count, not "n/required". ceiling: no student-scoped route exposes `required_approvals`. upgrade: add the denominator if a student-facing assignment summary route is ever added.

### No-trigger (frontend)

- `frontend/app/(app)/assessments/attempts/[id]/proctoring/page.tsx:75` — 2-item breadcrumb trail because Attempt has no title field; no condition named for adding the fetch.
- `frontend/app/(app)/assessments/[id]/edit/review/page.tsx:25` — same breadcrumb gap, no condition named.
- `frontend/app/(app)/batches/[id]/batch-name-context.tsx:5` — context threads the batch name from layout to one deep page instead of refetching; pure rationale, no revisit condition.
- `frontend/app/(app)/interview-exp/layout.tsx:1` — layout exists only to scope `revalidatePath`; explanatory, no revisit condition.
- `frontend/app/(app)/roadmap/[id]/page.tsx:35` — meta-refresh instead of client polling interval; explanatory, no revisit condition.
- `frontend/app/(app)/sheets/layout.tsx:1` — same `revalidatePath`-scoping pattern as interview-exp layout; no revisit condition.
- `frontend/components/batches/batch-avatar.tsx:94` — deterministic hash-based fallback color; explanatory, no revisit condition.
- `frontend/components/interview-exp/create-post-form.tsx:18` — comma-separated tags instead of a tag-chip widget; no condition named for upgrading.
- `frontend/components/labs/lab-fixed-console.tsx:90` — swapping edges reuses the current size number instead of separate remembered dims; no revisit condition.
- `frontend/components/labs/sandbox-terminal-panel.tsx:8` — hard cap of 4 sandbox terminals; justified by resource cost but no explicit "raise when X" condition.
- `frontend/lib/webauthn.ts:41` — enum cast bridging go-webauthn strings to DOM lib types; declared permanently safe, no revisit condition.
- `frontend/lib/webauthn.ts:80` — same cast justification, references the note above.

---

**37 markers, 22 with no trigger.**
